name: Rust
on: [push]

jobs:
  linux:
    name: Build Linux ${{ matrix.config.arch }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        config:
          - { arch: x86_64, bits: 64 }

    steps:
      - name: aaa
        run: |
          gcc --version
          cmake --version
